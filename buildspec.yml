version: 0.2

phases:
  pre_build:
    commands:
      - echo ${DB_NAME}
      - pwd
      - ls
  build:
    commands:
      - docker-compose build
artifacts:
  files:
    - '**/*'
  base-directory: .